server:
  port: 7777
spring:
  profiles:
    active: default
  application:
    name: config-server
eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


