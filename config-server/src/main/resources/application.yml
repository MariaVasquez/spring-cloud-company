server:
  port: 7777

spring:
  profiles:
    active: default
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MariaVasquez/microservice-config.git
          username: mm.vasquez
          password: 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
          default-label: main
          clone-on-start: true
eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
